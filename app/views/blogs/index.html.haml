%h1 Listing blogs
%div.notice= notice

%table
  %thead
    %tr
      %th Title
      %th(colspan="3")
  %tbody
    - @blogs.each do |blog|
      %tr
        %td= blog.title
        %td= link_to 'Show', blog
        %td= link_to 'Edit', edit_blog_path(blog)
        %td= link_to 'Destroy', blog, method: :delete, data: { confirm: "ブログ「#{blog.title}」を削除します。よろしいですか？" }
= link_to 'New Blog', new_blog_path
